# Ablation: 슬라이딩 윈도우 hop 크기 (overlap 비율)
# 작은 hop → 높은 overlap → 샘플 수 증가 (데이터 증강)
base_config: "configs/default.yaml"

sweep:
  - param: "sliding_window.hop_sec"
    values: [0.1, 0.2, 0.5]
    # 0.1s → 90% overlap, 0.2s → 80% overlap, 0.5s → 50% overlap

overrides:
  sliding_window.enabled: true
  sliding_window.window_sec: 1.0
  experiment.steps: [extract, train]
