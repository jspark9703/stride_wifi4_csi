# Ablation: Hampel 이상치 필터 활성화 여부
base_config: "configs/baseline.yaml"

sweep:
  - param: "preprocessing.hampel_enabled"
    values: [true, false]

overrides:
  experiment.steps: [preprocess, sanitize, extract, train]
